# Frozen next-rule selection

## Selection

Implement exactly one outer rule: **certified immediate Active three-prize Powerful Hand KO guard v1**.

The Sol-Ultra strategy judge selected this direction after verifying the frozen evidence packet and all listed report/raw hashes. It has two independent direct causal paths (`86974207`, `86981695`) and materially lower collateral risk than any one-replay multi-prompt alternative. The current submission's mature `11-10 / 643.7050` score and public-22 zero-difference shadow are replacement evidence only, not causal selection evidence.

Exact parent:

- `candidates/alakazam_exposed_dudunsparce_run_away_ready_alakazam_ko_transaction_v1/main.py`
- SHA-256 `CB52F1737417EAEEAEF226CFF79ABD4FA58119E3F2AF1D448DFBE5D68722E213`

Candidate destination:

- `candidates/alakazam_certified_active_three_prize_powerful_hand_ko_v1`

Implementation destination:

- `implementation/alakazam_certified_active_three_prize_powerful_hand_ko_v1`

## Positive transaction certificate

At an exact MAIN callback, choose the unique Powerful Hand attack before any optional hand-spending or Boss action only when every clause below is true:

1. The engine state is live, turn at least 2, no `looking` transaction is active, and the select envelope is exact `MAIN`, `minCount=maxCount=1`.
2. No inherited transaction owns the callback before or after the exact parent call. The merge quarantine is clear. Duplicate-callback behavior is deterministic.
3. Own Active is exactly one Alakazam with an exact public stack, exact max HP, clear status, and publicly certified energy units that pay Powerful Hand.
4. Own full hand is exact and `len(hand) == handCount`.
5. Opponent Active is exactly one public Pokemon with exact stack/max HP and `prize_count(target) == 3` after public Legacy Energy or tool modifiers.
6. Powerful Hand metadata is certified; the target and every public counter-placement/protection source are clear. Fail closed on Mist Energy, Rock Fighting Energy, prevention, status, HP ambiguity, unknown card metadata, or any visible effect that may change counter placement or KO.
7. Exactly one legal Powerful Hand option exists, with no ambiguous attack option encoding.
8. `20 * current exact hand count >= current exact target HP` before spending any optional card.
9. The selected action passes exact validity and a quarantined single-option rerun of the parent returns that same attack. Otherwise restore the parent's state and action exactly.

The rule is a public attack-to-KO-to-three-prize transaction certificate. It must not key on Mega Lucario's card name, team, episode, replay action, or opponent policy. It is stateless beyond duplicate-call caching because the immediate attack is the only overridden prompt; ordinary parent prize selection remains unchanged.

## Required positive anchors

Re-execute each state independently with fresh modules:

- `86974207`, target seat 0, steps `77` and `81`: choose the unique Powerful Hand attack with hand 19 into 380-HP, three-prize Mega Lucario ex instead of spending a hand card.
- `86974207`, step `110`: if the full certificate remains exact, attack the already Active 40-HP three-prize Mega Lucario instead of Boss. If any clause is not exact, document the failed clause rather than loosening it.
- `86981695`, target seat 0, step `138`: choose Powerful Hand into the 40-HP three-prize Active instead of Bossing one-prize Lunatone.

Every changed recorded position must be enumerated and inspected. No unlisted change may be accepted without a public-state explanation consistent with this one invariant.

## Mandatory negatives

- `86993519` steps `89`, `90`, `123`: Hero's Cape leaves 440 HP; `320/300/360 < 440`, so no trigger.
- `86992980` step `80`: Crustle is not a three-prize target and two Active Mist Energy remain; no trigger.
- `86987527` step `62`: Enhanced Hammer prompt, not MAIN; no trigger.
- `86990847` steps `42`, `67`: attach-first/draw-first routes are deferred and must not be added.
- `86991907` Boss target prompt: no same-turn certified three-prize Active KO; no trigger.
- A three-prize Active with Mist/Rock/protection, unclear stack/max HP, insufficient hand, unpaid attack, ambiguous attack option, non-clear status, or any inherited latch: exact parent identity.
- Existing direct-terminal, Active-Psychic-KO, current exposed-Dudunsparce, and every other historical transaction must remain behaviorally identical outside this predicate.
- Kaggle last-callable loader must resolve to public `agent`; no helper may be inserted after the final public `agent`.

## Rejected or deferred directions

- **Attach-first exposed-Dudunsparce KO transaction:** strategically coherent and multi-step, but only one direct narrow current state. Old `86978513` additionally needs evolution and is not the same predicate. Defer to the next cycle after this lower-risk probe.
- **Active attach plus fixed-draw KO:** one direct state and more resource/ability sequencing risk.
- **Unique-Active-Mist Hammer transaction:** one direct state; latest Crustle replay is a certified two-Mist negative.
- **Archaludon Boss friction / setup rules:** the two Archaludon losses share a broad tempo symptom but not one repeated narrow mechanism.

## Implementation and gate

Use one newly spawned Fast `ptcg_candidate_worker`. Copy the exact parent into the isolated destination and change only the outer rule, tests, and receipt there. Do not edit any existing candidate or package.

Required focused gate:

- compile/import and exact hashes;
- legal 60-card deck with exactly one ACE SPEC;
- Kaggle last-callable loader returns public `agent` and initial 60-card deck;
- all positives and negatives above;
- duplicate-callback identity, invalid-action count zero, cache-free tree;
- current public-22 both-seat shadow and historical anti-overfit shadow;
- packaged both-seat historical-Silver smoke.

Fixed paired evaluation must use identical opponents, seeds, games, and both seats for exact parent versus candidate. Require exact schedule equality, unique keys, zero action errors/max-step hits/duplicates, and no adjacent floor regression. Because this is a rare exact predicate, a `0 gain / 0 regression` aggregate is not improvement evidence but may permit a practical live probe only if every targeted changed state is correct and all retention gates pass.
