# Strategy selection: finalized Super Psy Bolt retreat KO bridge v1

Selected by Sol-Ultra and frozen by root on 2026-07-21 JST.

## Verdict

Implement exactly one isolated
`alakazam_finalized_super_psy_bolt_retreat_ready_alakazam_ko_bridge_v1`.
Defer the early Poké Pad Abra transaction. Do not stack either mechanism with
the rejected three-Prize family or the exploratory v5 source.

Direct implementation parent is
`candidates/alakazam_public_h0_h1_turn_objective_guard_v1/main.py`, SHA-256
`23B89E347565F1782F44494E8ACD89FAB0FEED20C484B2DA14DE1195B908CBE9`.
Formal rollback remains guarded Teleportation SHA-256
`4A95DCE0BB095A05F58085DFC450528C5939527E30B9D40E43A76B0CFCE2AE16`.
V5 is not a parent.

Root evidence is `ROOT_VERIFIED_EVIDENCE.md`, SHA-256
`502E3E884C143CB81AEB3104F56154F867F2EAA6614693DFAA50E7BE79B82175`.
The 38-row live CSV SHA-256 is
`29F8F4378BE61DF37243C69A0FBD3AB13094AD39817668907172508C1CC94360`.

## Hypothesis

When the complete H0/H1 pipeline's finalized ordinary MAIN action is the
unique Super Psy Bolt from a paid Active Kadabra, replace it with the inherited
strict-Prize-lead retreat transaction only when the sole ready Bench Alakazam
has a public-clear exact Powerful Hand KO.

This is not the rejected retreat-v1 early-ownership rule. The parent has
already finalized an attack, so the new start cannot preempt a remaining
productive PLAY, EVOLVE, ATTACH, ABILITY, Dawn, Boss, or Hilda action. Preserve
the accepted v3 finalized-END start unchanged.

## Frozen start contract

Run every inherited overlay and the entire H0/H1 decision pipeline first. New
ownership is allowed only when all inherited/recovery/Fez/Active-Psychic/
Enriching/H0-H1/guarded-Teleportation transactions have delegated, no latch
has just aborted on this callback, and the finalized action is exactly one
legal `ATTACK 1071` Super Psy Bolt.

The state must also prove all of the following:

1. exactly one complete positive-HP Active Kadabra;
2. retreat unused and exactly one legal RETREAT option;
3. positive publicly exact retreat cost with no visible modifier and one
   deterministic exact single-unit payment;
4. exactly one eligible Bench Alakazam, complete, positive HP, globally unique
   by serial/components, and already holding public Psychic Energy that pays
   Powerful Hand;
5. exactly one complete unchanged opposing Active, publicly clear against
   Powerful Hand;
6. exact current `20 * handCount >= target HP`;
7. positive public target Prize value, nonnegative post-KO Prizes, and strict
   post-KO lead: `own_prizes - target_prizes < opponent_prizes`;
8. `post_ko_prizes == 0 || deckCount > post_ko_prizes`;
9. complete hand, discard, Stadium, source, destination, target, component,
   and payment fingerprints with every protected serial globally unique.

Reuse the inherited stranded-retreat transaction and add an immutable
`start_mode=finalized_super_psy_bolt`. Do not change the existing END-mode
start or its accepted behavior.

## Frozen transaction stages

1. At MAIN freeze the finalized parent attack, source Kadabra, exact payment,
   unique destination Alakazam, target, hand/deck/Prize/Stadium state, and
   return RETREAT.
2. At `DISCARD_ENERGY`, select only the next frozen payment serial.
3. At `SWITCH` or `TO_ACTIVE`, select only the frozen Alakazam serial.
4. At returned MAIN, revalidate the complete transaction, require source now
   Bench, destination now Active, `retreated=true`, and exactly one legal
   Powerful Hand `1072`; attack.
5. Clear on resolution and delegate. Never own Prize selection.

Malformed metadata, duplicate/missing options, multiple ready destinations,
public-effect ambiguity, fingerprint/count change, damage shortfall, target
protection, Prize/deck failure, unexpected context, turn/seat/game change, or
stale state clears and delegates to exact parent. An aborted latch cannot
restart on the same observation. Repeated identical callbacks must return the
same cached action without advancing state.

## Positive and negative boundaries

Required positives, with first difference at the named row and full atomic
route through Powerful Hand KO:

- `87175722/S64`;
- `87170471/S73`.

Mandatory parent-identity negatives:

- Mist target `87176878/S94`;
- insufficient damage/Fez END `87166999/S142`;
- Dudunsparce states `87170471/S130` and `87169861/S95`;
- all nine broad Poké Pad rows, all three three-Prize H0 crossings;
- all 13 productive non-END retreat-v1 controls;
- every inherited v3 END route;
- every inherited active/delegated latch and malformed/duplicate mutation.

Do not modify Poké Pad/Hilda reserve logic, Run Away Draw, Dudunsparce
mobility, three-Prize H0 behavior, Boss targeting, base scores, v5
transactions, deck, or runtime semantics. Kaggle must still load this exact
agent as the only/last callable insertion.

## Practical probe gate

The user-authorized fast practical probe may proceed after major-breakage
checks, not formal adoption. Before packaging require:

- compile/import; unchanged legal 60-card deck with one ACE SPEC; source/
  runtime parity; loader-last callable; cache-free tree;
- both positives complete in exact checked-engine fixtures and every negative
  stays parent-identical;
- repeated callback, stale state, payment, promotion, target-clear, strict
  lead, deck clock, uniqueness, abort and rollback checks;
- current-38 plus historical callback-complete shadow against H0/H1 and formal
  rollback, every difference classified, zero invalid actions and duplicate
  mismatches;
- completed transaction proof in both semantic seats;
- package-local both-seat Historical-Silver smoke with zero action errors and
  max-step hits, plus clean archive inventory and hashes.

Formal adoption later requires at least +2 wins over both comparison anchors,
no seat/block decline, Historical-Silver improvement, no adjacent-opponent
decline, at least four natural complete activations spanning both seats and two
buckets, at least two mechanism-linked gains, zero regressions, and zero
structural faults. Score movement without an observed start is retention only.
