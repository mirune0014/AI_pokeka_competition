# Root record: user-directed reduction of validation scope

The deterministic full-corpus execution of
`PUBLIC_ROLE_COMPLETE_POKEMON_COMMITMENT_DOMINANCE_V1` was interrupted by the
root after the user correctly observed that the workflow was spending too much
time on repeated corpus validation and too little on live deployment.

- interrupted command was the one bound by
  `EXECUTION_SPEC_PUBLIC_ROLE_COMPLETE_POKEMON_COMMITMENT_V1.md`;
- destination was created at 2026-08-01 14:14 JST;
- only the copied `source_manifest.json` existed at interruption;
- no CSV or summary was complete;
- the Python process was explicitly terminated;
- the partial directory is preserved and is never interpreted, deleted,
  resumed, or reused.

The prior completed terminal census remains authoritative for its own evidence:
15/15 causal positions were independently and root-verified `GOOD_CAUSAL`, with
zero invalid actions or owner collisions.  A separate read-only focused scan of
the 700 parent Pokemon commitments found both correct public-role holds and
role-empty nonterminal attack opportunities.  Those focused examples authorize
implementation as an exploratory live probe under the user's requested
practice-first policy; they do not convert the interrupted numeric gate into a
pass.

The new practical gate is limited to compile/import, legal 60-card deck and one
ACE SPEC, final callable loader order, deterministic duplicate handling,
focused positive/negative public-state fixtures in both seats, valid actions,
cache-free packaging, and a small both-seat engine smoke.  Live replays, rather
than another full 25,880-callback census, decide repair or rollback.
