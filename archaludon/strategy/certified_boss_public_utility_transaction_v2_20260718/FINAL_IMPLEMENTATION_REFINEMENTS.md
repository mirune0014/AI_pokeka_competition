# Final implementation refinements

Fresh live-loss and read-only code audits tightened the same public-utility
certificate without adding episode or card-ID exceptions:

- resource-tax control rejects public self-mobility text that can move the
  target out of the Active Spot without paying retreat;
- when Boss reduces the hand and invalidates the optimistic legacy target, a
  deterministic independently certified immediate-KO target may be bound;
- the proposed near-KO branch is disabled fail-closed because all weakness,
  resistance and protection modifiers were not exactly certifiable;
- callbacks bind the complete public signature, reject negative/out-of-range
  option indices, and cannot return a stale cached target action.

Frozen implementation source SHA-256:
`CCADF1554EE85FBF048E4511C1B4147413154E529A87AEBD8719844545E5F692`.
