model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"

[agents]
# Keep fan-out bounded: the parent plans, direct children execute, and children
# cannot create another delegation layer.
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 1800
interrupt_message = true
