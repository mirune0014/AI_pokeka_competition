# Final rule judgment: Hero's Cape current-payable survival

## Verdict

`REJECT_FORMAL_PARENT__AUTHORIZE_ONE_CONDITIONAL_EXPLORATORY_LIVE_PROBE`

This is option **C**. It is a locality/safety permission for exactly one live
probe, not a strength finding, formal-parent promotion, or permission to stack
another rule.

The authorized source is only:

- candidate `main.py` SHA-256
  `0EDE7D1B58AC31F6E3C4F10093D79940F08F058B7F63148CC48A884B25D4972B`;
- deck SHA-256
  `08421AE98D080A1EE3BA28F93DA0A99C79287A2BC6F57529FDA2E4CA56CC7C6A`;
- direct formal parent SHA-256
  `F4B578453C9D953BE94885144DD50E05DBA4510DF3340BE27ADE07495896046E`;
- frozen contract SHA-256
  `ADE5F064CCDD62D9436AD5837EC138276ED347335A380DAE7A9CD1B3C38ACABB`.

## Verified facts used

- Root's implementation verification
  (`1FFB55C56CB827D0B210AB49E8BA8833B6723DA483E4179F5D3D5226D07FC6E3`)
  establishes a direct historical-Silver sibling, with only `main.py`
  changed and the other 11 runtime files parent-identical.
- Focused tests passed the source positive and negative, six transaction
  groups, and 19 fail-closed negative groups in both logical seats. The exact
  engine proves: no Cape plus Aura Jab KOs Duraludon; Cape plus the same Raging
  Hammer makes Aura Jab leave it at `100/230`; one Basic Fighting then makes
  Mega Brave payable and KOs it.
- The source explicitly records `E1_CAPE_LETHAL`; it does not claim next-turn
  safety.
- The 217-file correct-seat shadow covered 11,967 actionable callbacks and
  produced exactly one intended first difference, `88643491:77`, with zero
  certificate-external differences, zero faults, and all 61 frozen controls
  parent-equal.
- The independent audit
  (`087255A089045B478DACB297CEEF0D3A062009B5BDED1FF88E79735F5A0BF355`)
  and Root reconciliation
  (`411B114E42EFA8B71DAD852DAF90944923367387BF2BEC43E8E7D1CE9D41BFE9`)
  agree: parent = candidate `478/760`, historical anchor `100/200`, adjacent
  population `378/560`, zero gains, zero regressions, all 16 cells equal,
  `760/760` duplicate and parent-candidate byte-trace equality, and zero
  faults, action errors, exceptions, or max-step hits. The missing physical
  `panel` column was unambiguously reconstructed from frozen spec
  `E143A4C005EF236AE1E5D736DEEDA18EBBC0E28BD8EE5CE8C0986F48C602D47B`;
  it does not alter paired equality.
- The fixed matrix contains no natural rule activation. Therefore its parity
  is neutral safety evidence only.

## Integrated judgment

The coherent hypothesis remains:

> When the inherited unique Raging Hammer is a non-KO and every attack payable
> from the opposing Active's currently attached public Energy KOs 130 HP but
> not 230 HP, attach Hero's Cape to the same Active Duraludon and then execute
> that exact inherited attack, without displacing a terminal, Prize, KO, or
> forced-defense route.

The implementation matches that mechanism at the certified source and fails
closed around setup, board formation, evolution, retreat, targeting,
disruption, unknown effects, and unsupported damage/payment. It preserves the
attacker, target, attack, damage, Energy, hand/deck state, and Prize state
except for spending Cape. Thus setup, backup formation, finishing, and prize
arbitration are not broadly rewritten.

What is proved is only immediate same-attack survival against the `E0`
envelope. Extra attack continuity, a better prize exchange, and match
conversion are unproved. The main strategic risk is spending the unique ACE
SPEC on a one-Prize Duraludon instead of a later Archaludon ex; a routine Basic
Fighting attachment already crosses the source back to lethal. Gust,
evolution, switching, other acceleration, and future-board opportunity cost
remain outside the certificate.

Formal adoption fails decisively: `100/200 < 104/200` on the primary anchor
and `478/760 < 486/760` overall. There are zero completed fixed-panel
transactions, zero repeated trigger buckets, zero mechanism-owned
parent-loss/candidate-win conversions, and no natural both-seat evidence.
Adjacent and both-seat results are nonregressing but unexercised. Score parity
must not be described as strength.

Under the user's practical-probe policy, the focused counterfactual, exact
transaction checks, broad shadow locality, and fault-free fixed matrix are
sufficient to rule out a known destructive implementation defect. One bounded
live probe is the smallest remaining experiment that can measure natural
frequency and mechanism-owned consequences.

## Mandatory package and pre-write conditions

Authorization self-voids if any condition fails:

1. Build one fresh archive from the exact candidate hash above. Do not edit the
   source, deck, contract, or add H5, H6, H7-A, or any other sibling.
2. After clean extraction, require exactly the expected 12 runtime files,
   candidate/deck hashes above, all 11 non-`main.py` files byte-identical to
   the formal parent, and zero caches, test artifacts, or generated files.
3. Recheck compile/import, loader entry point, exact 60-card legal deck with
   one ACE SPEC, deck request, and deterministic packaged-agent smoke in both
   seats. Require zero start failures, invalid actions, action errors,
   exceptions, nondeterminism, and max-step hits; duplicate-smoke traces must
   be byte-identical.
4. Record the extracted inventory, package manifest, archive SHA-256, and
   package-validation outputs before the write. Any hash or inventory drift
   requires a fresh evaluation and judgment, not an ad hoc repair.
5. Root must refresh authenticated Kaggle status, current and preceding
   submission maturity, exact episode-ID set, score path, and UTC-day quota.
   Confirm an available slot, no duplicate prior submission of this exact
   source/archive, and no recovering submission that the current policy
   requires preserving. Keep exact historical-Silver ready as rollback.
6. Root alone may make one write, clearly labelled as an exploratory Hero's
   Cape probe with no local strength gain. Never resubmit the exact
   source/archive.

## Live monitoring and stop conditions

Shadow every new correct-seat callback against the exact formal parent.
Checkpoint near 1, 5, 10, 20, and 40 public games, and make the final causal
assessment only after at least three hours. Enumerate every first action
difference; parent-identical games and aggregate score movement are zero Hero
evidence.

Immediately stop and reject/rollback on any Root-verified:

- package, validation, start, execution, legality, action-error, exception,
  nondeterminism, or max-step failure;
- stale transaction or state leakage across retry, turn, seat, or game;
- first difference outside the complete public certificate, including
  displacement of a KO, terminal, Prize, setup, or forced-defense route;
- wrong Cape serial/target, non-Cape-only confirmation, HP change other than
  exact `+100/+100`, changed attacker/target/payment/damage, or failure to
  follow confirmed Cape with stored Raging Hammer `224`;
- unsupported or ambiguous Energy, attack, status, Tool, Stadium,
  Weakness/Resistance, modifier, protection, or effect entering an accepted
  certificate;
- mechanism-owned causal regression, including a loss created by spending
  Cape here when the inherited attack preserves the game or when the lost ACE
  SPEC opportunity is later decisive.

Ordinary losses, a weak aggregate score, and unrelated matchup defects are
memos, not Hero regressions. A valid `E1_CAPE_LETHAL` opponent attachment is
also not by itself a defect; reject only if the changed Hero line is proved
causally worse.

If no certificate-valid activation occurs by the final three-hour/roughly
40-game review, close
`UNINFORMATIVE__SAFE__REJECT_FORMAL_PARENT__RETAIN_INACTIVE`, never resubmit,
and move to a mechanism-distinct experiment. A valid activation without
causal gain remains inactive. Any mechanism-owned regression rejects the rule.

## Exact evidence needed next

For this probe: immutable package evidence, the post-write submission and
validation IDs, a prior-ID set difference, every new correct-seat replay,
parent/candidate callback shadows, accepted-certificate telemetry (`E0`,
`E1_BASIC`, serials, scores, stages/resets), execution-fault counts, and an
exact parent counterfactual for every changed game.

Formal reconsideration would additionally require all frozen gates: at least
`104/200` anchor and `486/760` overall, adjacent at least `378/560`, no
seat/cell/floor regression, at least four certified completed transactions
across two board configurations with at least two per seat, repeated
`E1_CAPE_LETHAL` exposure without causal loss, and at least two Root-verified
mechanism-owned parent-loss/candidate-win conversions covering both seats,
with trace proof that Cape survival created the extra continuity and gain.
If live produces no trigger, the next discriminating test is a frozen
trigger-rich deterministic panel; a second submission of this source is not.
