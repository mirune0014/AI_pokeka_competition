# Next Archaludon hierarchy gap after Boss rank 11

Date: 2026-07-30 JST  
Scope: read-only strategy judgment

## Verdict

`NO_IMPLEMENTATION`

Select exactly one direction for the next evidence-collection cycle:

`PUBLIC_TWO_BRANCH_NEXT_OWN_TURN_TERMINAL_PACKAGE_RESERVATION_V1`

This is option **(d), broader winning-out / multi-turn turn-plan
commitment**. It is not authorization to edit a candidate. The sole
Root-verified source, episode `88824363` row `112`, has not yet proved either
the required public search-access certificate or the complete alternate
engine transaction. Until both are proved, the source is a provisional
positive only and must remain parent-identical in executable policy.

## Verified facts used

- Exact historical-Silver remains the formal strength and rollback anchor:
  `main.py`
  `F4B578453C9D953BE94885144DD50E05DBA4510DF3340BE27ADE07495896046E`;
  deck
  `08421AE98D080A1EE3BA28F93DA0A99C79287A2BC6F57529FDA2E4CA56CC7C6A`.
  Its deck plan is Cinderace setup/Turbo Flare into Duraludon, Archaludon-ex
  Assemble Alloy, three-Metal Metal Defender, backup preservation, selective
  non-ex Coated Attack, Full Metal Lab/Hero's Cape survival, and Boss prize
  conversion. Its local scorer does not reserve a plan across an
  opponent-forced discard and the following opponent turn.
- Frozen eight-rule cumulative v2 is
  `DEE5092B6785DF7A63752C7AAE497051D08DCC62F7723CD9786225A0C5A99DE8`.
  Its order is H2, search-aware terminal, H1, H5 v2, repaired H4, H6 v2,
  Hero, H3 v2, then exact Silver. Boss-ledger integration is authorized only
  as dormant rank 11 by judgment
  `20E440642D3178259EB18EBDD03749C7F807EFCE5BCF98F19B4298EA3DAFDBB9`;
  no completed, immutable combined-source/evaluation authority was supplied
  for this judgment.
- The completion audit
  `2AAC1BD209BC6008CA505AD7D0554A672911C514D069E1B5D22A1002E18CCE8B`
  keeps multi-turn winning outs, mode arbitration, threat envelopes, and
  Bench/future value open. The cumulative policy amendment
  `253B1A73E9415B937843F5DA93DF74350EBDA8193D7D9131003BFF62ABCA5B13`
  permits compatible dormant integration only after isolated destructive
  gates.
- Root's `88824363` evidence
  (`F9DB34FD4FEA6A2837578B5331CE15ADE3042490BC195832D217B044092C5B08`)
  verifies an opponent-forced exact two-card discard with our two Prizes
  remaining. Exact Silver discarded Boss `1182#38` plus Ultra Ball
  `1121#21`; Ice Cream `1147#36` plus Hero's Cape `1159#37` was legal and
  would retain Boss and both Ultra Balls. The public board held a
  `160/210` two-Prize Fezandipiti ex `140#118`, a three-Metal Active
  Archaludon ex, and one-Energy Cinderace/Duraludon development. The recorded
  continuation later demonstrated search/evolution/Alloy/attack components,
  but not the alternate retained-Boss transaction. The controlling memo is
  `17D65ED6EACC21841F8278E25FC2807E08DAFA9549FEC3949E20849288910DE3`.
- The `88247531:115` both-seat engine fork is a mandatory negative: saving
  the damaged three-Energy Bench body changed Metal Defender `253` for `220`
  into Raging Hammer `224` for `80`, an immediate `140`-damage and public
  two-hit-route regression.
- Boss-ledger dormancy report
  `15272B9C8BE97FEF955F3076D02051CDC2E96C00D1765037968A9C51A3A622C2`
  records `42,159` callbacks and zero complete opportunities. The only
  one-held/three-publicly-discarded Boss inventory was a correct terminal
  exclusion. This does not support option (e).
- The best alternatives remain less implementation-ready: `88826681`
  (`1AF4FB...F6D9`) and `88826155` (`3D6163...626D`) depend on unproved
  healing/retreat/protection or hidden Boss access; the two comeback-Boss
  sources (`18174B...A555D`) depend on hidden escape access; the Dragapult
  threat-window source has no executed alternate. They remain mode/threat
  research, not hard positives.

## Why this one direction

The selected mechanism is the only remaining candidate that can connect
hand/deck resources, board formation, attacker and backup readiness, Energy,
attack continuity, exact Prize value, and finishing in one public plan. It
would correct a structural weakness—irreversible local discard scoring can
destroy the intersection of resources needed by every surviving terminal
branch—rather than imitate an opponent or patch an episode.

It is still unsafe now. At `88824363:112`, an Archaludon-ex search hit on the
next own turn is not yet certified from public conservation/count evidence,
and the opponent-response interval can change promotion, retreat, board, and
target legality. An engine replay of the realized continuation alone cannot
become an opponent-policy assumption.

## Conditional behavioral contract

This contract becomes implementation authority only after the pre-edit gate
below passes and a fresh strategy judgment changes `NO_IMPLEMENTATION`.

### Public mode and observables

At a mandatory own-hand discard caused during the opponent's turn, build a
public plan graph from:

- game epoch, logical seat, first player, turn/action count, current context,
  effect/card text, exact legal options, and complete own hand with unique
  positive serials;
- own public board, discard/lost/revealed zones, Prize/deck counts, Stadium,
  status, Tool, Energy, attachment/retreat/supporter flags, and a
  conservation-checked known-card ledger;
- opposing public Active/Bench HP, Prize value, Energy, Tools, status,
  payable printed attacks, retreat cost, and public continuous effects; and
- audited deterministic damage/payment and count-only access bounds.

Opponent identity, episode/row/seed, hidden opponent hand, hidden deck order,
Prize identities, future draw identities, realized replay actions, or learned
action labels are forbidden.

The rule may preserve resources only when it proves two public branches:

1. **attacker survives:** retained Boss plus the unchanged payable attack
   takes exactly the remaining Prizes from one visible target; and
2. **attacker is KO'd by a currently payable public attack:** a certified
   promotion/search/evolution/Alloy/retreat/Boss/exact-attack chain rebuilds
   an attacker and takes exactly the remaining Prizes on the next own turn.

The preserved package is the intersection of resources required by all
certified surviving branches. Every search must have a deterministic
known-access certificate or a separately frozen count-only threshold; v1 may
not use an unspecified future draw identity. Every discarded alternative
must be proved unnecessary for current survival, retreat, attack payment,
backup formation, disruption recovery, and any higher-ranked route.

### Emitted action and state

- On the provisional source, the only admissible first difference is the
  semantic discard `{Ice Cream 1147#36, Hero's Cape 1159#37}` instead of
  `{Boss 1182#38, Ultra Ball 1121#21}`. Positions are never stored.
- Initial state is `CLEAR`. A valid emission creates only
  `AWAIT_DISCARD_CONFIRMATION`, storing the public snapshot, reserved
  resources, expendable resources, both branch digests, target/attack
  semantics, and option multiset.
- An identical retry returns the cached semantic pair without a parent call
  or state advance.
- A novel observation must prove exactly the selected expendable cards moved
  hand-to-discard and the reserved package remained. It then clears the
  actionable owner and may retain only a passive, read-only
  `WAIT_NEXT_OWN_TURN` plan digest.
- The passive digest never chooses an opponent-turn or forced-promotion
  action. At each novel public mutation it revalidates; at the next own Main
  it may arm a normal single-owner terminal transaction only if one complete
  branch remains exact.
- Missing/mutated options, incomplete hand, duplicate serial, conservation
  failure, unsupported effect, opponent-board divergence, target loss,
  access below threshold, attack/payment change, turn/seat/result/reset,
  exception, invalid action, or emergency fallback clears the plan. After an
  irreversible discard, rollback is logical only: delegate exact Silver from
  the actual state and never replay or undo a card.

### Exact cumulative precedence and collisions

If later admitted, insert the new rule at rank 6:

1. exact-parent direct terminal action;
2. one already valid active transaction owner;
3. H2 response-free last-Prize transaction;
4. search-aware same-turn terminal transaction;
5. H1 unique ready terminal-threat defense;
6. this next-own-turn terminal-package reservation;
7. H5 v2 paid lethal/no-ready-successor;
8. repaired H4 exact inherited-attack higher-Prize conversion;
9. H6 v2 attack-completing Energy reservation;
10. Hero same-attack survival;
11. H3 v2 line formation;
12. passive Boss last-copy consumer;
13. exact historical-Silver fallback.

An existing owner always prevents arming. Ranks 3-5 suppress this rule;
this rule suppresses clear ranks 7-12 only after its complete certificate.
Equivalent proposals have the higher-ranked owner. For a simultaneous Boss
ledger proposal, the ledger prepass runs once and remains passive: the new
rule may read a detached snapshot but cannot mutate it. The same semantic
discard is owned by rank 6; a different discard pair involving the protected
Boss fails closed unless a dedicated collision fixture proves both
certificates. Equal/unknown ranks, two-owner state, a newly appearing
different action after an irreversible step, or any speculative state
mutation clears all actionable state and returns the actual-state parent
action. In an all-eligible clear fixture H2 wins.

## Hard positives and mandatory negatives

The only provisional positive is `88824363:112`; it becomes positive only
after the exact gate below. Both logical seats, serial remapping, reversed and
duplicate-equivalent option order, identical retry, attacker-survives, and
attacker-KO/rebuild branches are required.

Mandatory parent-identical negatives include:

- `88247531:115` and any plan reducing inherited attack ID, damage, current
  Prize conversion, or ready successor;
- `88814136` and `88827776`, already owned by same-turn terminal rules;
- `88826681`, `88826155`, `88824894`, `88814688`, and `88820060`, whose
  benefit needs an opponent-response, hidden access, or mode assumption;
- `88819392`, owned only by the rank-12 last-copy Boss ledger;
- all rejected H3 v1, H4 v1/v2, H5 v1, and H6 v1 controls;
- no exact remaining-Prize target, no payable stored attack, no public
  promotion/retreat path, uncertain search access, incomplete hand, ambiguous
  alternative, alternative with survival/attack/setup value, immediate
  terminal parent action, supporter/attachment conflict, protected target,
  unsupported damage/effect, or an already active transaction.

## Falsifiable gates and exact evidence needed next

### 0. Mandatory pre-edit public-access and engine gate

Root must first produce a hash-bound row-112 public inventory showing every
Archaludon-ex copy as known, public-unknown, or inaccessible and recompute the
search lower bound without reading hidden deck/Prize identities. If no
deterministic or preauthorized count-only certificate exists, stop:
`SOURCE_FAIL__NO_IMPLEMENTATION`.

Then fork the exact seed/decks at the forced discard in both logical seats,
with serial remapping and option permutations:

- control: Boss plus one Ultra Ball;
- alternate: Ice Cream plus Cape;
- unchanged opponent agent; no forced replay actions;
- prove discard confirmation and both public survival/KO envelopes;
- on the KO branch, prove the actual legal promotion, Ultra Ball payment and
  search, Duraludon evolution, Assemble Alloy choice and exact Metal targets,
  retreat, Boss, Fezandipiti target, stored Metal Defender `253`, `220`
  damage, final two Prizes, and terminal completion;
- on the survival branch, prove the shorter Boss/target/same-attack terminal;
- include target removal, search miss, protection, changed Stadium, changed
  promotion, disrupted hand, and malformed-state fail-closed branches.

Require zero invalid actions, exceptions, stale/passive owners,
nondeterminism, action errors, nonzero exits, or max-step hits. Any changed
attack, target, damage, Prize result, missing step, or opponent-policy forcing
keeps `NO_IMPLEMENTATION`.

### 1. Isolated focused and shadow gates

Only after gate 0 passes may a fresh isolated direct child of exact Silver be
built. Require the source branches above, at least 30 public-state negatives,
both seats, retries/resets/duplicates, and exact semantic rollback.

Freeze a Root-hashed replay union containing all existing 261 sources, latest
verified public losses, every component positive, and every rejected control.
The only initial action difference may be the certified forced discard.
Recorded-parent continuation may cause one expected confirmation reset but no
second action difference. Any extra natural trigger stops for replay audit;
it is not silently accepted. Require zero trigger-external differences and
zero faults/max-step hits.

### 2. Immutable local numerical gate

Run the checked fixed 760 schedule with exact Silver as isolated baseline:
200 historical-anchor games, 560 adjacent games, both 380-game seats,
identical seeds and traces, 760 unique keys, duplicate controls, and
`max_steps=1000`.

Safety floors are candidate at least `478/760`, historical at least
`100/200`, adjacent at least `378/560`, seat 0 at least `243/380`, seat 1 at
least `235/380`, no parent-win loss, no opponent/seat/repeated-bucket floor
loss, and zero action errors, exceptions, nonzero exits, duplicate mismatch,
or max-step hit. Root and an independent Sol-Ultra evaluator must recompute
the raw rows. Equality is safety only, not strength.

### 3. Cumulative collision and integration gate

First freeze and Root-verify the Boss-rank-11 combined parent. Then integrate
the unchanged isolated component as one new rank only. Preserve every nine
existing positive and negative. Test all 45 unordered pairs across ten rules,
180 ordered both-seat clear collisions, both active-owner directions,
same/different actions, retry, reset, exception, post-discard confirmation,
passive-ledger observation, and all-eligible cases. Require zero dual/stale
owners, owner switches, speculative mutation, unknown collision, attribution
error, or fault.

Repeat the immutable schedule directly against the frozen Boss-integrated
parent, plus the full combined shadow and both-seat engine/package smoke.
Any direct-parent regression or mechanism mismatch rejects integration.

## Regression risks and adoption boundary

The chief risks are preserving Boss/search at the cost of Cape/healing,
assuming a hidden search hit, losing setup or backup formation, binding a
future promotion incorrectly, consuming the wrong Alloy Energy, carrying
stale state across the opponent turn, and colliding with H6 or the Boss
ledger after an irreversible discard. The rule can also overvalue an apparent
terminal plan that disruption breaks.

No live probe, package, or formal-parent claim follows from this report.
Practical adoption later requires absolute strength, primary-anchor movement,
both-seat safety, repeated mechanism-valid behavior in at least two board
configurations, adjacent-population and repeated-bucket safety, zero
action/max-step faults, and trace proof that gains came from preserved
terminal packages. The next discriminating experiment is exactly gate 0; if
it fails, collect a second Root-verified forced-resource callback with
deterministic public search/access and a complete next-turn terminal branch
before reconsidering option (d).
