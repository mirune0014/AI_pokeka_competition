# External Assets

Put large or competition-provided files here when needed.

Recommended layout:

- `external/ptcg_engine/`: unpacked official `ptcg_engine.zip` from the Kaggle competition Data page.

This directory is intentionally excluded from git because the engine source is competition-provided material.
