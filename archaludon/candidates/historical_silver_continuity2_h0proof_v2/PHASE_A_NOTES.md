# Continuity2 v1 — Phase A boundary

Implemented in this phase:

- deterministic slot-aware option and lineage identities;
- a JSON-serializable H0 / H1-survive / H1-after-KO / H2 public-state plan;
- exclusive physical-card and turn-budget reservations;
- visible payable-response profiles with separate attack damage, placed counters,
  and Bench spread, failing closed for unsupported payable effects;
- explicit Mysterious Rock Inn and Cornerstone Stance outgoing blocks;
- the verified Jumbo Ice Cream survival breakpoint, including eligible non-ex
  Archaludon;
- module-local latest trace plus opt-in JSONL, and deterministic exception fallback.

The following items were deliberately deferred when Phase A was frozen:

- Assemble Alloy `ACTIVATE` / `ATTACH_TO` / repeated `ATTACH_FROM` binding;
- Turbo Flare exact count and repeated target binding;
- retreat Energy-payment callbacks and the actual promoted lineage;
- evolution/recovery prerequisite execution beyond their public ledger inventory.

They are implemented and separately verified in Phase B; see
`PHASE_B_NOTES.md`. This file remains the historical Phase A boundary.
