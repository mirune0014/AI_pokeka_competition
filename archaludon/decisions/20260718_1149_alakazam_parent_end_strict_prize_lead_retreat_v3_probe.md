# 2026-07-18 11:49 JST - Alakazam strict-Prize-lead retreat bridge v3 live probe

## Decision before external write

Permit exactly one practical Kaggle probe of
`alakazam_parent_end_strict_prize_lead_retreat_ko_bridge_v3` after the final
immediate submission/quota refresh. This consumes live play as part of the
evaluation, as requested by the user. It is not permanent baseline promotion.
The exact live parent remains frozen as rollback and causal anchor.

## Isolated hypothesis

When the live parent has finalized `END` with a stranded Active, v3 may execute
one complete same-turn conversion only if all public facts certify the exact
route:

`RETREAT -> exact Psychic payment -> ready Alakazam promotion -> Powerful Hand KO`.

The KO must leave v3 **strictly ahead** in remaining Prizes. This excludes the
known Rmy `3:3` tie regression while retaining the observed live `1:3` rescue.
Setup, gust, attachment, evolution, Ability, and all non-END parent decisions
keep precedence.

Target: convert certified immediate-KO turns that the parent ends because the
wrong Active is stranded. Retention targets: Alakazam Rmy, exact
Historical-Silver, both seats, all non-END parent callbacks, and the complete
public setup/resource/prize plan.

## Frozen candidate and paired evidence

- candidate source/runtime/deck SHA-256:
  `49E954F53E043CD79D928DFB2340CCB5D85A17F33CCB824C871CCE153C0C9C95` /
  `9CA7A415451B84343165EF8A5CDC0D67BBF1B4969A13C984C09B142B4898CD9A` /
  `7B413177E5077777F2178143839C0155B03B92BBC8B3A6607621A7D43F351141`;
- exact live-parent source SHA-256:
  `FAB47771161EF7F43C9402B58D38FF240C92B6A2B77FFA6B925DFEA7F990D033`;
- immutable fixed schedule: 144 unique, exactly equal paired keys;
- parent/v3: `84/144 -> 86/144`;
- P0/P1: `45/72`, `41/72`; known/fresh: `44/72`, `42/72`;
- gains: orders `34` and `140`; regressions: zero;
- all nine changed traces are the exact four-step transaction above;
- Rmy order 144 is callback-for-callback parent-identical and remains a P1 win;
- row, command, action, max-step, malformed, missing, extra, and duplicate
  errors: zero;
- root verification SHA-256:
  `A65A8771E1B8A161C280189D70AEB6765F36382F53305B539FF9528F8A6AE06A`;
- independent numerical audit SHA-256:
  `052A0281F05657421A9928160BD73FA06664FAF2290AC1C7BF7C169E9EA08906`;
- final ACCEPT-for-one-probe strategy judgment SHA-256:
  `1A7B252742E828300631D74C22C229FBA97BA68B7366DDE982009D31FD57202E`.

The paired estimate is intentionally treated as a safety/causal gate, not as
proof of a stable rating gain. Its approximate 95% interval includes zero and
all observed activations are P1. The live probe must establish whether the
latch activates and whether the Prize lead survives the opponent reply and
backup-attacker clock.

## Clean package gate

- archive:
  `packages/alakazam_parent_end_strict_prize_lead_retreat_ko_bridge_v3_20260718/submission_alakazam_parent_end_strict_prize_lead_retreat_ko_bridge_v3_20260718.tar.gz`;
- archive SHA-256 / bytes:
  `EA89F12A995001C214F7FF00D4AF91F091F39323799C332961FA4656D94C5C5C` /
  `2,009,474`;
- package manifest SHA-256:
  `4D35AEB02CAEDE572D69E3294661310A17708CF3209678FE0C084E01A750924D`;
- exactly 12 regular archive files, with zero absolute, traversal, cache, or
  bytecode members;
- stage and clean re-extraction: 12 files, 5,609,779 bytes, byte-identical;
- isolated import, callable agent, deterministic exact initial deck, legal 60
  cards, card IDs, copy caps, and one ACE SPEC: PASS;
- packaged smoke versus exact Historical-Silver: P0 `117` steps and P1 `130`
  steps; both start and complete as candidate wins; zero action errors and no
  max-step hit;
- frozen candidate/stage/re-extraction source and deck hashes: exactly equal;
- package cache artifacts: zero.

The first smoke invocation used the repository working directory and stopped
before game start on the source layout's relative `deck.csv` import. No game
row was produced and no package byte was changed. The corrected clean-package
procedure used the extraction as import working directory and passed both
seats as recorded above. This was a harness configuration error, not a hidden
action failure.

## Authenticated external refresh at 11:40-11:43 JST

Raw snapshot:
`live/54794301/presubmit_strict_prize_v3_20260718_1140`.

- CLI, API, episode listing, raw extraction, and replay download exits: all
  zero;
- frozen prior episode CSV SHA-256:
  `D38F021C04A882AD792F8EEE90A7CBF129A95D0F8D1C21694A7D0D8EF99D314D`;
- current episode CSV SHA-256:
  `D48593E619897BF94D849124F238331C65903D519858D694EAD812657B08B6C0`;
- current rows/unique IDs: `41/41` (`40` public, `1` validation);
- prior/current/new IDs: `15/41/26`; prior-only IDs: zero;
- all 26 requested replays downloaded once, parsed as JSON, and matched their
  requested `info.EpisodeId`; errors: zero;
- new public results: `14-12`; total public results: `22-18`;
- current submission `54794301`: `COMPLETE`, public score `686.7`;
- current API/CLI UTC `2026-07-18` source rows: `0/0`; root quota
  interpretation: `0/5` used, five slots available;
- API/CLI raw SHA-256:
  `989AB6A8F0DD18D66DAD093FCF3FCA889897E3234BE4481877FAB1847A4218EC` /
  `19FAB77A067D8AD6CD7DDF1F6D931D8AE9DB9B2D0CBB21573E99107BB8949CCB`;
- raw collection receipt SHA-256:
  `27A76B274EC526A0D1F26170E9F37B4CA4823E6F9D2245B85F9079298BADBE01`;
- final recursive inventory: `57` rows, SHA-256
  `76BE5C03B6AF68970AA4DA6CFD3B1EFF76451E3CAB54DE672D5FFE9026D318A8`.

The live parent remains complete and well below 1000. The 26-new-game window
ended at `686.7569` after a `14-12` sequence and does not establish recovery
toward the protection threshold. The candidate is valid, packaged, causally
distinct, and locally non-regressing; one practical live slot is justified.

## Immediate-write condition and post-submit protocol

Immediately before upload, root must re-read authenticated submissions and
confirm: no new UTC-day row, target status remains COMPLETE, archive/source
hashes remain frozen, and no duplicate v3 filename exists. Use the ASCII
description:

`Alakazam strict-prize retreat KO v3; 86/144; practical live probe`

After upload, do not retry on a terminal-print error until CLI/API reads prove
whether a row exists. Record the single new submission ID, timestamp, bytes,
filename, description, status, and post-write quota. For every new episode,
check v3 latch activation before attributing any score. If it activates,
inspect post-KO reply and backup continuity; if it never activates, treat the
score only as retention evidence and keep the separate public attack-target
conversion hypothesis next.

## Immediate write check at 11:49:59-11:50:16 JST

- authenticated CLI/API/list-episodes/package checks: all exit zero;
- CLI/API submission rows: `50/50`;
- current target: `54794301`, `COMPLETE`, score `686.7`;
- UTC `2026-07-18` source rows: CLI/API `0/0`, therefore `0/5` used;
- exact v3 filename matches before upload: CLI/API `0/0`;
- current episode rows/unique IDs: `41/41`;
- literal new/missing IDs versus the 11:43 snapshot: `0/0`;
- immediate episode CSV/JSON SHA-256:
  `D48593E619897BF94D849124F238331C65903D519858D694EAD812657B08B6C0` /
  `5E8FA67A2AF829FD6441F1F004448780313D92325D35D7F563628BF7E1E8B80C`;
- frozen archive/source/deck SHA-256 remained
  `EA89F12A995001C214F7FF00D4AF91F091F39323799C332961FA4656D94C5C5C` /
  `49E954F53E043CD79D928DFB2340CCB5D85A17F33CCB824C871CCE153C0C9C95` /
  `7B413177E5077777F2178143839C0155B03B92BBC8B3A6607621A7D43F351141`;
- archive bytes/cache artifacts: `2,009,474 / 0`.

All immediate-write conditions pass. The external write is authorized now.

## Submission receipt

- submission ID: `54797361`;
- timestamp: `2026-07-18T02:50:41.757Z` (`2026-07-18 11:50:41.757 JST`);
- uploaded bytes: `2,009,474`, exactly equal to the frozen archive;
- filename:
  `submission_alakazam_parent_end_strict_prize_lead_retreat_ko_bridge_v3_20260718.tar.gz`;
- description:
  `Alakazam strict-prize retreat KO v3; 86/144; practical live probe`;
- initial Kaggle status: `PENDING`, empty score and error description;
- immediate post-write CLI/API matches: exactly `1/1`;
- immediate post-write UTC-day source rows: CLI/API `1/1`; root quota
  interpretation: `1/5` used, four slots remain;
- no retry and no duplicate upload.

The transfer reached 100%, after which the Windows Kaggle CLI returned exit
`1` while printing its accented confirmation text (`UnicodeEncodeError` under
CP932). As required, root did not retry. Immediate independent CLI and API
reads both showed the single new row above, and the API-reported byte count
matched the frozen archive exactly. The external write therefore succeeded;
the CLI error was post-upload terminal rendering only.
