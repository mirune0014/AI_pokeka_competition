# Data Workspace

Suggested local layout:

- `data/episodes/`: public episode JSON datasets downloaded from Kaggle.

The scripts in `tools/` can analyze files in this directory, but the raw datasets are intentionally excluded from git.
