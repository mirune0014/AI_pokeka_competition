# Root freeze: Night Stretcher causal repair

## Decision

The post-shadow behavior contract is frozen for implementation.

- Original strategy SHA-256:
  `70B0FFD9482BA5E23F5D2200713171810B9C30F250D7762EF23CF7A87C8B8D0F`
- Controlling post-shadow amendment SHA-256:
  `34D37F172FAD3BFED743121284519D565510C850AB5DC90466E340BA36F808B9`
- Rejected diagnostic source SHA-256:
  `8232D0AD9C93012210A702558C06FA66C390B4AA53619A5F8FED7A8229FF555B`
- Historical-Silver direct parent SHA-256:
  `558EE5DB29E001428B0D59813613FABEE8E15B96E422746C104BDFAB4DC22DB6`
- Unchanged deck SHA-256:
  `08421AE98D080A1EE3BA28F93DA0A99C79287A2BC6F57529FDA2E4CA56CC7C6A`

The single hypothesis is:

> Purpose-bound Night Stretcher may alter a recovery target or continuation
> only when a complete, exact public-state certificate proves a strict
> full-route improvement.  Otherwise it preserves the current conversion,
> exact parent target/action, and public resources.

This freeze authorizes implementation and focused verification only.  It does
not accept a transient source, authorize fixed evaluation, or justify a Kaggle
write.

## Required post-implementation gates

1. Freeze the final source hash only after focused checks complete.
2. Verify the exact direct-parent prefix, unchanged deck, final callable loader,
   compile/import, legal 60 cards with one ACE SPEC, deterministic duplicate
   handling, and a cache-free tree.
3. Pass all named focused positives and negatives from the controlling
   amendment in both seats.
4. Run a new complete shadow over the immutable 207-replay / 209-target-seat
   corpus.  The rejected `8232` run and interrupted `47BF` run are diagnostic
   only.
5. Root-inspect every first action difference, every changed recovery target,
   every changed route step, and every parent-win/candidate-loss row.
6. Proceed to the immutable fixed-760 schedule only if the full shadow meets
   the original frequency, ownership, validity, and qualitative gates.

## Root acceptance of the strategy audit

The final strategy audit found no remaining behavior-contract blocker after
the root incorporated:

- exact-win / whole-board terminal-loss / nonterminal-KO precedence;
- meaningful-effect checks for all Stretcher-enabled attacks;
- mode-aware hand-Metal necessity;
- explicit no-attack defensive evolution lifecycle;
- copy-isomorphic callback-time parent-target rebinding;
- conservative parent-sequence deference and stale-queue invalidation.

The remaining uncertainty is implementation fidelity, not hypothesis scope.
