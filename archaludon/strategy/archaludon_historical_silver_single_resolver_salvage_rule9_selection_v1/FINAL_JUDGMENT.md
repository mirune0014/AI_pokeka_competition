# Rule 9 final judgment

## Verdict

`DEFER-DORMANT`.

- Rule 9 is not integrated or widened.
- Accepted Rule 5 (`D966E455E5110F9D5616195AEAAC8663E6A92F310DAFC4B7F79E4D37149A9C62`)
  remains the parent.
- Rule 9 fixed760 is forbidden. A larger outcome matrix cannot repair the
  failed per-rule mechanism-activity gate.

## Evidence

- Focused fixtures: 35/35.
- Replay shadow: 30,977 callbacks, no Rule 9 action difference; entry telemetry
  was not persisted.
- Fixed160: Rule 5 = Rule 9 = 100/160; paired gains/regressions/ties 0/0/160.
- Both seats, Historical-Silver anchor, adjacent opponents, and every
  opponent/seat cell are unchanged.
- All 160 candidate traces are byte-identical to their Rule 5 traces.
- Process faults, action errors, max-step hits, duplicate mismatches, and
  nonzero exits: zero.
- Independent trace audit found 236 Gear reveal prompts and six physical
  same-turn Gear/Boss/switch/attack shapes, but zero transaction satisfying
  the frozen all-remaining-Prize entry certificate.

The fixed raw format did not serialize the Rule 9 owner. Because entry returns
the same Gear action as Rule 5, entry starts remain unknown rather than a
measured zero. The independently required complete natural transaction count is
nevertheless zero proven, below the frozen minimum of one.

## Decision reasoning

The observed policy is mechanically neutral, but the intended mechanism was
not exercised. Unsupported reveal-to-empty behavior, ledger aborts, and caught
wrapper exceptions therefore remain unmeasured. Observable parity is not a
substitute for the frozen mechanism-coverage gate, and conditions must not be
broadened after zero proven completions.

The execution operator's `.venv-rl` versus requested `py -3.11` outer prefix
deviation is recorded in both numerical reports. Both interpreters are Python
3.11.6, and frozen inputs, inner manifests, schedule and outputs validated, so
the deviation does not invalidate the parity evidence.

## Controlling reports

- Independent numerical audit SHA-256:
  `CE316741F15BFC55F9B4810C97CBBD7D98371BEF4D6BEAAB80BE58C1626B7BE1`.
- Root fixed160 recomputation SHA-256:
  `71228FE5460C3FAC3EBEA06226A6A8D728E5EA4F612FB160A31BDF5AE04FB484`.

