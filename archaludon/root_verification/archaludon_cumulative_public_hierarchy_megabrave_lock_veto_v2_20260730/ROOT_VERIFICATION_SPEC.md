# Root verification specification

Date: 2026-07-30 JST

Candidate name:
`archaludon_cumulative_public_hierarchy_megabrave_lock_veto_v2`

## Frozen authorities

- direct cumulative source parent:
  `archaludon_cumulative_public_hierarchy_after_search_aware_v1`
- direct parent `main.py` SHA-256:
  `BE8C67E387CE4F36344A4B0DE610CAB9976BB07EE49200997D35CCC6C5DBF18A`
- formal rollback historical-Silver `main.py` SHA-256:
  `F4B578453C9D953BE94885144DD50E05DBA4510DF3340BE27ADE07495896046E`
- exact deck SHA-256:
  `08421AE98D080A1EE3BA28F93DA0A99C79287A2BC6F57529FDA2E4CA56CC7C6A`
- cumulative integration policy SHA-256:
  `F8E81D3872C809477068E7C9B476302BE20C14001127EA308C4C80B4CB95BB66`
- cumulative integration selection SHA-256:
  `2797D1C3B590E369FF3B38B20D2783ADAF1223FB0056759AAAEE69AFC453D942`
- rejected cumulative fixed-760 CSV SHA-256:
  `B58EBC8CF088B9B740651E8058478838D27EB1D0205A1E8CFB4C303276340BB4`
- independent numerical audit SHA-256:
  `837919D3A3EBC1BAE9558BED2EF81B7C7A6449C28B0A7CD0E9B3A21791679CE8`
- changed-trace qualitative audit SHA-256:
  `3C71161C542737732C4F4F2529DDB59BC2F931A9539DDAE8EBFA522BD3F35814`
- controlling repair judgment SHA-256:
  `A263871FFB639DB2BB0A535642CFE534434E838ADC1665BE31637FEA66DC112B`

The rejected direct parent is never eligible for Kaggle submission. The formal
rollback remains exact historical-Silver.

## Only permitted behavioral change

Implement `H4_PUBLIC_MEGA_BRAVE_SELF_LOCK_VETO_V1`.

Before H4 creates or mutates a transaction, H4 is ineligible when the tracked
immediately preceding public opponent attack is Mega Brave `983`. H4 must then
own no transaction, spend no Boss, and propose no action. Arbitration continues
normally; the demonstrated state must retain the exact parent's Metal Defender
attack.

Do not change any other rule, precedence rank, collision behavior, parent
scoring, reset semantics, deck, or non-`main.py` runtime file. Do not add an
opponent, seed, episode, serial, or replay-future exception.

## Root structural and behavioral checks

Root independently verifies:

1. direct-parent and child hashes before and after every test;
2. only `main.py` differs from the direct cumulative parent;
3. compile/import, exactly one loader-last `agent`, legal 60 cards, one ACE
   SPEC, no unknown or excess non-Basic-Energy copies, and zero cache members;
4. locked Mega Brave negatives in both seats, two serial permutations, and two
   option orderings;
5. no-lock controls for `None` and Aura Jab `982`;
6. duplicate callbacks, reset/rollback, lower-rule collision, and all previous
   H4 positives and negatives;
7. all component suites, collision registry, union replay shadow, all four
   prior changed traces, and isolated-component semantic/certificate equality;
8. zero invalid actions, exceptions, emergency fallbacks, stale/two-owner
   states, owner switches, retry parent calls, max-step hits, or
   certificate-external differences.

The old Mega Lucario seat-1 seed `271958318` first divergence must disappear.
The historical-Silver seat-0 seed `271828201`, Arch Shumpei seat-1 seed
`271958328`, and Mega Lucario seat-0 seed `271958329` changes must remain
mechanically unchanged.

## Immutable fresh fixed-760 gate

Use the exact prior 760 schedule, engine, opponents, seats, seeds, games per
cell, and result convention. Write to a new immutable destination. Never
overwrite or reinterpret the rejected candidate's raw directory.

The strict prediction is:

- overall: historical-Silver `478/760`, repaired child `478/760`;
- historical anchor: `100/200` each;
- adjacent population: `378/560` each;
- seat 0: `243/380` each;
- seat 1: `235/380` each;
- gains/regressions: `0/0`;
- Kangaskhan/Crustle: at least `28/80`, with neither seat below its parent;
- duplicate controls: exact on all 760 keys;
- action errors, missing starts, nonzero exits, and max-step hits: zero;
- exactly three outcome-neutral changed traces;
- Mega Lucario seat 1 seed `271958318`: result, step count, and trace
  parent-identical.

Any unexplained additional trace or outcome change blocks the live probe.

After execution, one `ptcg_local_evaluator`, Root recomputation, and one
`ptcg_sol_ultra_worker` final practical-live judgment are required.

## Package and live gate

Only Root may build the authoritative clean archive or write Kaggle. A fresh
archive must contain exactly the 12 runtime files, match the frozen source and
deck, pass compile/import/deck-request and both-seat extracted-package smoke,
and contain no cache, test, generated, or evidence files.

Immediately before a write, Root refreshes authenticated submissions, UTC-day
quota, status/score, and exact episode-ID set. The description must name the
cumulative eight-rule hierarchy and Mega Brave self-lock veto.

Passing this specification permits one exploratory live probe only. It is not
formal-parent promotion or a strength claim.
