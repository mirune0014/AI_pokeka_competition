# Archaludon hierarchy coverage matrix

This matrix prevents a successful narrow candidate from being mistaken for
completion of the full user-agreed decision hierarchy. Exact
historical-Silver remains the formal parent until a candidate independently
earns adoption.

Latest exact-win update: Sol-Ultra selected
`SEARCH_AWARE_ACTIVE_TERMINAL_BEFORE_NONTERMINAL_BOSS_V1`, contract SHA
`DE30DBA76E39DC0F6FF922E24727A3A9013C266DCEAC5CC306E6581CE601FDB0`.
Root's mandatory pre-edit gate passed 8 same-turn terminal transactions and
80 callbacks across both logical seats, public-only `P(hit)=164/165`, two
search-miss/Boss-preserving fallbacks, five fail-closed branches, and zero
faults. Raw output SHA is
`0EFAB0FECAB5FFAEFE904322C79ACD762A5069E08AE6671D0FEC34E91BB03CF4`.
One isolated direct-parent implementation is authorized and in progress; this
supersedes the older table cell saying its no-Boss engine branch remains
unexecuted.

| Required mechanism | Current evidence / candidate | Status |
|---|---|---|
| Exact terminal-win hard gate | Historical parent has tactical attack/Boss rules; H2 reserves one exact public same-turn terminal line. Root verified the six-stage exact-engine win, full shadow, neutral fixed-760 safety gates, clean package, independent numerical audit, final Sol-Ultra judgment, and a completed live probe. New Root-bound sources `88814136` and `88827776` expose search-aware terminal routes that generic Pokégear/Boss and Boss scoring miss. | Partial; H2 completed 41 public games at 23-17-1 and 769.8075, but no H2-owned public trigger occurred. `88827776` proves every Ultra Ball/evolution/Alloy/220 component on the realized turn, but its exact no-Boss branch remains a mandatory pre-edit engine gate. |
| Exact terminal-loss / forced-defense gate | H1 removes one unique visible ready terminal Alakazam response at exact 3/2. Through the 08:15 checkpoint it had 35 public games, 19-16, but zero H1-parent action differences across every replay callback, including the latest 11-game 4-7 block. | Narrow live probe; score fell to 736.4650 on the parent path, with no public trigger, causal H1 evidence, or H1 implementation defect. |
| Prize-route arbitration | H1 covers equal-Prize terminal-threat removal; H2 covers one exact last-Prize transaction. H4 v1 preempted setup and was rejected. H4 v2 restricted arbitration to callbacks where exact historical-Silver already chose Attack, but one route substituted a different attack after Boss and was rejected. H4 v3 locks Boss and the KO calculation to the exact inherited attack ID. | Partial; H4 v3 completed focused tests, both-seat exact-engine transactions, exact shadow, duplicate controls, fixed-760, root recomputation, independent numerical audit, and final judgment. It is contract-correct and fixed-safe with no strength gain, retained only as an inactive sibling. |
| Harmful-KO avoidance | H1 proves one case where the KO target, not taking a Prize itself, determines survival. The frozen 48-loss audit rejected seven hidden-access or unproved multi-turn examples. New episode `88826681` is a Root-verified public mirror Prize race where a non-terminal one-Prize Boss KO yields first hit on the opposing two-Prize Active; attacking Active and retaining Boss is legal, but healing/protection/retreat keep match causality unproved. | General hard rule remains incomplete. `88826681` is a high-quality narrow mode hypothesis, not permission to globally decline KOs. |
| Non-KO exposure and Active constraint | H7-A remains lower-quality and inactive. The bounded Hero's Cape sibling implements `HERO_CAPE_CURRENT_PAYABLE_SAME_ATTACK_SURVIVAL` directly from exact historical-Silver at source `88643491:77`. Root verified source/negatives, six transaction groups, all three exact-engine branches, 217-file/11,967-callback shadow with one intended difference, fixed760 equality, clean package, and both-seat smoke. Its authorized live probe `55083165` matured after three hours at 41 public games, `21-20`, score `843.5788`. | Closed as `UNINFORMATIVE__SAFE__REJECT_FORMAL_PARENT__RETAIN_DORMANT`. Root replayed all 41 games in the correct seat: 2,332 callbacks, zero starts, differences, invalid actions, exceptions, or stale transactions. It supplied no mechanism-strength evidence, but is implementation-safe and may remain as a dormant rule in a later integrated live candidate under the integration policy below. |
| Bench damage and future-board value | Replay `88247531` identifies survival-by-evolution of an invested Bench target. Root-verified `88824894` adds a tied Boss choice where removing one Roserade projects a future public damage threshold from `320` to `290`, but the future attacker and alternate Garchomp route were not fixed at the decision. | Not implemented; `88824894` is a bounded future-value source, not a certified match counterfactual. |
| Visible ready and one-to-two-turn threats | H1 covers one already-ready visible terminal threat. H3 v2 implements and root-verifies the line-formation side of `88684114` without assuming hidden Mega Lucario or Gravity Mountain, and delegates whenever a legal Explorer route exists. Opponent one-turn completion envelopes remain unimplemented. | Partial; H3 v2 completed its three-hour live probe as submission `55070349`. Through 33 public games every live callback remained parent-identical, so the `14-19` record and 571.6682 score are not H3 causal evidence. Retain H3 inactive. |
| Known hand and deterministic access | No persistent public-search/access ledger is implemented. Root-verified `88819392` proves the last publicly accessible Boss was discarded despite a plan-equivalent alternate pair; later Unfair Stamp prevents match-causal attribution. | Not implemented; `88819392` supplies a concrete public-zone lifetime/reset fixture. |
| Card-count/effect-only probabilistic access | H3 v2 implements the first count-only hypergeometric access certificate for Ultra Ball finding Duraludon at `P >= 0.999`, plus a deterministic Basic-Metal lower bound. Root source audit confirmed no hidden deck/Prize read. It requires public proof that discarding Boss does not abandon an exact same-turn conversion and vetoes the override whenever Explorer is legal. | Partial; H3 v2 was parent-identical at 478/760 with zero regressions. Its completed 33-game live probe had no natural trigger, so frequency and causal value remain unproved; retain inactive. |
| Winning / normal / comeback modes | No explicit mode state with separate thresholds is implemented. Root-verified `88826155` shows generic anti-donk setup creating a hidden-Boss final-Prize liability despite visible Active survival; `88826681` shows a non-terminal Boss KO losing the first-hit Prize race. | Not implemented; both are mode/risk sources with hidden counterfactual branches, not deterministic hard rules. |
| Winning outs and forced conversion | H2 is one exact six-action winning-out transaction. Root-verified `88827776` supplies a same-turn search/evolve/attack terminal source, and `88814136` supplies a Pokégear/Boss terminal search source. | Partial; the new routes require checked no-diversion engine branches before implementation. |
| Turn-plan commitment | H1/H2 use observed-state transactions with snapshot, rollback, duplicate handling, and reset. H3 v2 completes a nine-callback setup line from Ultra Ball through Turbo Flare Energy placement in both seats. Root-verified `88824363` exposes forced-discard terminal-package loss; `88827776` exposes a search-aware Active-terminal plan preempted by Boss. | Partial; generic certified reservation remains unimplemented, and both new plans need exact engine branch proof. |
| Higher-Prize same-turn conversion | H4 v1's broad `Boss -> unique higher-Prize KO` rule caused four fixed-schedule regressions and was rejected. H4 v2 restricted it to inherited-Attack callbacks and restored all results, but one natural/fixed route changed inherited Raging Hammer `224` into Hammer In `223` after Boss. H4 v3 stores, evaluates, and later executes only the exact inherited attack ID. Contract: `STRATEGY_SELECTION_H4_V3.md`, SHA `644981CD0157B9C2D673BDE9CEFDABE2BBE0AA0532B5651D78A0884B56850A68`. | H4 v1/v2 rejected. H4 v3 is completed, contract-correct, fixed-safe, and retained as an inactive sibling. Fixed-760 remained `478/760` with zero gains or regressions, so final judgment rejects formal-parent adoption, packaging, and live submission. |
| Attack-completing Energy reservation | H6 v1 directly from exact historical-Silver reserved visible Metal `#120` across the exact Ultra Ball discard/search/attach/attack transaction at `88584180:90-114`, but Root reproduced a destructive certificate breach: a second Basic Metal obtained before attachment did not invalidate the reservation. Final judgment rejected v1 and forbids packaging/submission. H6 v2 is the direct-parent minimal repair. Every pre-attachment continuation requires a complete visible hand, exact `handCount`, positive unique serials, exactly one Basic Metal, and the stored serial. Root independently reran structure, 6 positive, 58 negative, 20 rollback, 7 reset/exception, 30 sibling controls, 64 new both-seat uniqueness cases, 9 full transactions, the defect reproducer, a 253-file/14,007-callback prewrite shadow, fresh fixed-760, and clean-package both-seat smoke. The sole prewrite difference remained intended row `88584180:91`; all certificate-external callbacks were parent-identical and faults zero. | H6 v1 is permanently rejected. H6 v2 completed its single live probe `55077607` and the planned 40-game maturity review at `23-17`, exact score `742.9763`. Two later public losses moved it to 42 games and `734.7427`; both added 122 correct-seat callbacks and remained H6-parent-identical with zero faults. H6 v2 is implementation-safe but supplies no causal strength evidence, so it is retained only as an inactive sibling and is not the formal parent. |
| Non-ex 120 tactical breakpoint | H5 v1 directly from exact historical-Silver converted a certified inherited Raging Hammer non-KO into a same-turn one-Prize Coated Attack KO, but fixed-760 regressed `478 -> 477`. H5 v2 adds the missing urgency separator: a paid deterministic lethal attack on the surviving current Active and no opposing Bench Pokemon with a payable printed attack. H5 v2 passed 60/60 focused checks, both-seat exact-engine transactions, a 207-file shadow with one intended difference, and fixed760 at parent=candidate `478/760` with 0 gains/regressions and 760/760 trace identity. | H5 v1 is an immutable rejected control. H5 v2 completed its live probe as submission `55073442` at 47 public games and exact score 778.4843. Every live action was parent-identical with no H5 defect or causal evidence. Preserve it inactive and never resubmit its exact source/archive. |

## Current execution order

1. H6 v2 is closed as
   `UNINFORMATIVE__SAFE__NO_CAUSAL_DIFFERENCE__REJECT_FORMAL_PARENT__RETAIN_INACTIVE`.
   Its 40-game maturity evidence is
   `live/55077607/maturity_20260729_2107/ROOT_MATURITY_VERIFICATION.md`.
   Never resubmit it or use it as the formal parent.
2. Hero's Cape is closed as
   `UNINFORMATIVE__SAFE__REJECT_FORMAL_PARENT__RETAIN_INACTIVE`.
   Its maturity evidence is
   `live/55083165/maturity_20260730_0127/ROOT_MATURITY_VERIFICATION.md`.
   Never resubmit or use it as the formal parent.
3. Retain completed H4 v3 as an inactive sibling. Do not package, submit, or
   use it as a formal parent. Its final judgment is
   `ACCEPT_IMPLEMENTATION_REJECT_PROMOTION_RETAIN_INACTIVE`.
4. Retain H5 v1 only as the rejected regression control. Never package,
   submit, stack, or use it as a parent.
5. H5 v2 is implementation-valid but not the formal parent. Its completed
   47-game live probe had no natural trigger; retain it inactive.
6. Never package or submit H6 v1. Its pre-attachment unique-Metal certificate
   is broken even though its fixed schedule was outcome-neutral.
7. H6 v2 closes that exact defect, passed all local/package gates, used its
   single authorized live write, and is now closed as a non-promoted inactive
   sibling.
8. Parent-identical H6 score movement remains zero H6 evidence.
9. The independently verified Hero's Cape sibling is frozen at source SHA
   `0EDE7D1B58AC31F6E3C4F10093D79940F08F058B7F63148CC48A884B25D4972B`.
   Never resubmit that exact source/archive and never use it as the formal
   parent. It may be retained unchanged as a dormant component in a new
   cumulative live candidate.
10. Select and first verify exactly one next mechanism directly against exact
    historical-Silver from `NEXT_HYPOTHESIS_EVIDENCE_BUNDLE_20260730.md`.
    After its isolated destructive-safety gate passes, the live candidate may
    integrate multiple previously verified rules, including dormant Hero's
    Cape. Integration must record the firing rule, suppressed competing rules,
    precedence decision, exact-parent action, final action, rollback reason,
    and duplicate/reset state. A rule collision must fail closed to the exact
    historical-Silver action unless an explicit tested precedence certificate
    resolves it.
11. Update this matrix after every final accept/reject judgment.

The goal remains active while any required row is incomplete or lacks
destructive-fault-free live verification where a live probe is needed.
